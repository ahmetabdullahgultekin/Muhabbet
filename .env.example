# Muhabbet — Production Environment Variables
# Copy this to .env and fill in real values. NEVER commit .env to git.

# Database
DB_USERNAME=muhabbet
DB_PASSWORD=CHANGE_ME_strong_password_here

# Redis
REDIS_PASSWORD=CHANGE_ME_strong_password_here

# JWT (generate with: openssl rand -base64 64)
JWT_SECRET=CHANGE_ME_generate_with_openssl_rand_base64_64

# MinIO
MINIO_ACCESS_KEY=CHANGE_ME
MINIO_SECRET_KEY=CHANGE_ME_strong_password_here

# Netgsm SMS
NETGSM_USERCODE=your_netgsm_usercode
NETGSM_PASSWORD=your_netgsm_password

# Sentry
SENTRY_DSN=https://your-sentry-dsn-here

# Firebase (place firebase-adminsdk.json in project root)
FCM_CREDENTIALS_PATH=/app/firebase-adminsdk.json
